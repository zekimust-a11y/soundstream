# Create stable paths for FLIRC input devices so systemd can open them reliably.
#
# FLIRC exposes multiple input interfaces. We care about the *keyboard* event device
# (the one that emits KEY_F9/KEY_F10 for volume).
#
# Check devices with:
#   ls -l /dev/input/by-id/
#   sudo udevadm info -a -n /dev/input/eventX | head -n 80
#
# FLIRC keyboard interface (flirc.tv flirc Keyboard)
KERNEL=="event*", SUBSYSTEM=="input", ATTRS{idVendor}=="20a0", ATTRS{idProduct}=="0006", ATTRS{name}=="flirc.tv flirc Keyboard", SYMLINK+="input/flirc-kbd", MODE="0660", GROUP="input"


