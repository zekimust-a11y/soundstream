// Layout constants shared across desktop/web screens.

// `DesktopSidebar` is fixed width on large web layouts.
// If screens size their grids using full `windowWidth` instead of available content width,
// the rightmost column can get clipped.
export const DESKTOP_SIDEBAR_WIDTH = 320;



